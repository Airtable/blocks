<!-- Generated by documentation.js. Update this documentation by updating the source code. -->

### Table of Contents

-   [@airtable/blocks][1]
    -   [BlockSdk][2]
    -   [GlobalConfig][3]
    -   [GlobalConfigValue][4]
    -   [Viewport][5]
-   [@airtable/blocks/models][6]
    -   [fieldTypes][7]
    -   [generateGuid][8]
    -   [viewTypes][9]
    -   [recordColoring][10]
    -   [settingsButton][11]
    -   [AbstractModel][12]
    -   [AbstractModelWithAsyncData][13]
    -   [Aggregator][14]
    -   [Base][15]
    -   [Cursor][16]
    -   [Field][17]
    -   [QueryResult][18]
    -   [Record][19]
    -   [Table][20]
    -   [View][21]
    -   [Watchable][22]
-   [@airtable/blocks/ui][23]
    -   [CSS & external scripts][24]
    -   [Color utilities][25]
    -   [React hooks for working with Airtable data][26]
    -   [expandRecord][27]
    -   [expandRecordList][28]
    -   [expandRecordPickerAsync][29]
    -   [globalAlert][30]
    -   [AutocompletePopover][31]
    -   [Button][32]
    -   [CellRenderer][33]
    -   [ChoiceToken][34]
    -   [CollaboratorToken][35]
    -   [ColorPalette][36]
    -   [ConfirmationDialog][37]
    -   [Dialog][38]
    -   [FieldIcon][39]
    -   [FieldPicker][40]
    -   [Icon][41]
    -   [Input][42]
    -   [Link][43]
    -   [Loader][44]
    -   [Popover][45]
    -   [ProgressBar][46]
    -   [RadioSynced][47]
    -   [RecordCard][48]
    -   [RecordCardList][49]
    -   [Select][50]
    -   [SelectButtons][51]
    -   [TablePicker][52]
    -   [Toggle][53]
    -   [Tooltip][54]
    -   [ViewPicker][55]
    -   [ViewportConstraint][56]

## @airtable/blocks

### BlockSdk

Import the SDK from `'@airtable/blocks'`.

#### base

Represents the current Airtable [Base][15].

Type: [Base][57]

#### cursor

Returns information about the active table, active view, and selected records.

Type: [Cursor][58]

#### globalConfig

Storage for this block installation's configuration.

Type: [GlobalConfig][59]

#### installationId

Returns the ID for the current block installation.

Type: [string][60]

##### Examples

```javascript
import {installationId} from '@airtable/blocks';
console.log(installationId);
// => 'blifDutUr92OKwnUn'
```

#### localStorage

Wrapper for [`window.localStorage`][61] which will automatically fall back to in-memory storage when
`window.localStorage` is unavailable.

Type: (Storage | InMemoryStorage)

##### Examples

```javascript
import {localStorage} from '@airtable/blocks';
localStorage.setItem('lastScrollTop', 0);
```

#### models

Contains the model classes, field types, view types, and utilities for working with record coloring
and record aggregation.

Type: any

#### reload

Call this function to reload your block.

Type: function (): void

##### Examples

```javascript
import React from 'react';
import {UI, reload} from '@airtable/blocks';
function MyBlock() {
    return <UI.Button onClick={() => reload()}>Reload</UI.Button>;
}
UI.initializeBlock(() => <MyBlock />);
```

#### sessionStorage

Wrapper for [`window.sessionStorage`][62] which will automatically fall back to in-memory storage
when `window.sessionStorage` is unavailable.

Type: (Storage | InMemoryStorage)

##### Examples

```javascript
import {sessionStorage} from '@airtable/blocks';
sessionStorage.setItem('lastScrollTop', 0);
```

#### settingsButton

Controls the block's [settings button][11].

Type: SettingsButton

#### UI

React components, hooks, and UI helpers.

Type: any

#### viewport

Controls the block's viewport. You can fullscreen the block and add size constrains using
`viewport`.

Type: [Viewport][63]

### GlobalConfig

**Extends Watchable**

A key-value store for persisting configuration options for a block installation.

The contents will be synced in real-time to all logged-in users of the installation. Contents will
not be updated in real-time when the installation is running in a publicly shared base, or in
development mode.

Any key can be watched to know when the value of the key changes.

You should not need to construct this object yourself.

#### Examples

```javascript
import {globalConfig} from '@airtable/blocks';
```

#### canSet

Returns `true` if the current user can set the global config value at `key`, `false` otherwise.

##### Parameters

-   `key` **([string][60] \| [Array][64]&lt;[string][60]>)** A string for the top-level key, or an
    array of strings describing the path to the value.

##### Examples

```javascript
import {globalConfig} from 'airtable-block';

if (globalConfig.canSet('favoriteColor')) {
    globalConfig.set('favoriteColor', 'purple');
}
```

Returns **[boolean][65]** `true` if the current user can set the global config value at `key`, and
`false` otherwise.

#### canSetPaths

Returns `true` if the current user can perform the specified updates to global config, `false`
otherwise.

##### Parameters

-   `updates` **[Array][64]&lt;{path: ([string][60] \| [Array][64]&lt;[string][60]>), value:
    [GlobalConfigValue][66]}>** The paths and values to set.

##### Examples

```javascript
import {globalConfig} from 'airtable-block';

const updates = [
    {path: ['topLevelKey1', 'nestedKey1'], value: 'foo'},
    {path: ['topLevelKey2', 'nestedKey2'], value: 'bar'},
];
if (globalConfig.canSetPaths(updates)) {
    globalConfig.setPaths(updates);
}
```

Returns **[boolean][65]** `true` if the current user can perform the specified updates to global
config, `false` otherwise.

#### get

Get the value at a path. Throws an error if the path is invalid.

##### Parameters

-   `key` **([string][60] \| [Array][64]&lt;[string][60]>)** A string for the top-level key, or an
    array of strings describing the path to the value.

##### Examples

```javascript
import {globalConfig} from 'airtable-block';

const topLevelValue = globalConfig.get('topLevelKey');
const nestedValue = globalConfig.get(['topLevelKey', 'nested', 'deeply']);
```

Returns **[GlobalConfigValue][66]** The value at the provided path.

#### set

Sets a value at a path. Throws an error if the path or value is invalid.

##### Parameters

-   `key` **([string][60] \| [Array][64]&lt;[string][60]>)** A string for the top-level key, or an
    array of strings describing the path to set.
-   `value` **[GlobalConfigValue][66]** The value to set at the specified path.

##### Examples

```javascript
import {globalConfig} from 'airtable-block';

if (globalConfig.canSet('favoriteColor')) {
    globalConfig.set('favoriteColor', 'purple');
}
```

Returns **AirtableWriteAction&lt;void, {}>**

#### setPaths

Sets multiple values. Throws if any path or value is invalid.

##### Parameters

-   `updates` **[Array][64]&lt;{path: ([string][60] \| [Array][64]&lt;[string][60]>), value:
    [GlobalConfigValue][66]}>** The paths and values to set.

##### Examples

```javascript
import {globalConfig} from 'airtable-block';

const updates = [
    {path: ['topLevelKey1', 'nestedKey1'], value: 'foo'},
    {path: ['topLevelKey2', 'nestedKey2'], value: 'bar'},
];
if (globalConfig.canSetPaths(updates)) {
    globalConfig.setPaths(updates);
}
```

Returns **AirtableWriteAction&lt;void, {}>**

### GlobalConfigValue

Type: (null | [boolean][65] \| [number][67] \| [string][60] \|
[Array][64]&lt;[GlobalConfigValue][66]> | [Object][68]&lt;[string][60], [GlobalConfigValue][66]>)

### Viewport

**Extends Watchable**

Information about the current viewport

#### Examples

```javascript
import {viewport} from '@airtable/blocks';
```

#### addMaxFullscreenSize

Add a maximum fullscreen size constraint. Returns a function that can be called to remove the
fullscreen size that was added. Use .maxFullscreenSize to get the aggregate of all added
constraints. Both `width` and `height` are optional - if either is set to null, that means there is
no max size in that dimension.

##### Parameters

-   `$0` **\$Shape&lt;ViewportSizeConstraint>**
    -   `$0.width`
    -   `$0.height`

Returns **UnsetFn**

#### addMinSize

Add a minimum frame size constraint. Returns a function that can be called to remove the added
constraint. Use .minSize to get the aggregate of all added constraints. Both `width` and `height`
are optional - if either is null, there is no minimum size in that dimension.

##### Parameters

-   `$0` **\$Shape&lt;ViewportSizeConstraint>**
    -   `$0.width`
    -   `$0.height`

Returns **UnsetFn**

#### enterFullscreenIfPossible

Request to enter fullscreen mode.

May fail if another block is fullscreen or this block doesn't have permission to fullscreen itself.
Watch `isFullscreen` to know if the request succeeded.

#### exitFullscreen

Request to exit fullscreen mode

#### isFullscreen

Can be watched.

Type: [boolean][65]

Returns **[boolean][65]**

#### isSmallerThanMinSize

Type: [boolean][65]

Returns **[boolean][65]**

#### maxFullscreenSize

The maximum dimensions of the block when it is in fullscreen mode. Returns the smallest set of
dimensions added with [Viewport#addMaxFullscreenSize][69].

If `width` or `height` is null, it means there is no max size constraint on that dimension. If
`maxFullscreenSize` would be smaller than [Viewport#minSize][70], it is constrained to be at least
`minSize`.

Type: ViewportSizeConstraint

Returns **ViewportSizeConstraint**

#### minSize

Can be watched. The minimum dimensions of the block - if the viewport gets smaller than this size,
an overlay will be shown asking the user to resize the block to be bigger. Returns the largest set
of dimensions added with addMinSize. If `width` or `height` is null, it means there is no minSize
constraint on that dimension.

Type: ViewportSizeConstraint

Returns **ViewportSizeConstraint**

#### size

Can be watched.

Type: {width: [number][67], height: [number][67]}

Returns **{width: [number][67], height: [number][67]}**

## @airtable/blocks/models

### fieldTypes

#### Examples

```javascript
import {fieldTypes} from '@airtable/blocks/models';
const numberFields = myTable.fields.filter(field => field.type === fieldTypes.NUMBER);
```

### generateGuid

Helper to generate a GUID

#### Examples

```javascript
import {generateGuid} from '@airtable/blocks/models';
const id = generateGuid();
```

Returns **any** string

### viewTypes

#### Examples

```javascript
import {viewTypes} from '@airtable/blocks/models';
const gridViews = myTable.views.filter(view => view.type === viewTypes.GRID);
```

### recordColoring

#### recordColoring.modes

create a record coloring mode object

##### Examples

```javascript
import {recordColoring} from '@airtable/blocks/models';

// no record coloring:
const recordColorMode = recordColoring.modes.none();
// color by select field:
const recordColorMode = recordColoring.modes.bySelectField(someSelectField);
// color from view:
const recordColorMode = recordColoring.modes.fromView(someView);

// with a query result:
const queryResult = table.selectRecords({recordColorMode});
```

#### recordColoring.modes.bySelectField

##### Parameters

-   `selectField` **[Field][71]**

#### recordColoring.modes.byView

##### Parameters

-   `view` **[View][72]**

#### recordColoring.modes.none

#### recordColoring.ModeTypes

#### recordColoring.ModeTypes.BY_SELECT_FIELD

#### recordColoring.ModeTypes.BY_VIEW

#### recordColoring.ModeTypes.NONE

#### recordColoring.RecordColorMode

Type: ({type: any} | {type: any, selectField: [Field][71]} | {type: any, view: [View][72]})

#### recordColoring.RecordColorModeType

Type: \$Values&lt;any>

### settingsButton

**Extends Watchable**

Interface to the settings button that lives outside the block's viewport.

Watch `click` to handle click events on the button.

#### Examples

```javascript
import {settingsButton} from '@airtable/blocks';
settingsButton.isVisible = true;
settingsButton.watch('click', () => {
    alert('Clicked!');
});
```

#### isVisible

Whether the settings button is being shown. Set to `true` to show the settings button. Can be
watched.

Type: [boolean][65]

Returns **[boolean][65]**

### AbstractModel

**Extends Watchable**

Abstract superclass for all models.

#### Parameters

-   `baseData` **BaseData**
-   `modelId` **[string][60]**

#### id

The ID for this model.

Type: [string][60]

Returns **[string][60]**

#### isDeleted

True if the model has been deleted.

In general, it's best to avoid keeping a reference to an object past the current event loop, since
it may be deleted and trying to access any data of a deleted object (other than its ID) will throw.
But if you keep a reference, you can use `isDeleted` to check that it's safe to access the model's
data.

Type: [boolean][65]

Returns **[boolean][65]**

### AbstractModelWithAsyncData

**Extends AbstractModel**

Abstract superclass for all block SDK models that need to fetch async data.

#### Parameters

-   `baseData` **BaseData**
-   `modelId` **[string][60]**

#### isDataLoaded

Type: [boolean][65]

Returns **[boolean][65]**

#### loadDataAsync

Will cause all the async data to be fetched and retained. Every call to `loadDataAsync` should have
a matching call to `unloadData`.

Returns a Promise that will resolve once the data is loaded.

#### unloadData

#### unwatch

Unwatching a key that needs to load data asynchronously will automatically cause the data to be
released. Once the data is available, the callback will be called.

##### Parameters

-   `keys` **(WatchableKey | [Array][64]&lt;WatchableKey>)**
-   `callback` **[Function][73]**
-   `context` **[Object][68]??**

Returns **[Array][64]&lt;WatchableKey>**

#### watch

Watching a key that needs to load data asynchronously will automatically cause the data to be
fetched. Once the data is available, the callback will be called.

##### Parameters

-   `keys` **(WatchableKey | [Array][64]&lt;WatchableKey>)**
-   `callback` **[Function][73]**
-   `context` **[Object][68]??**

Returns **[Array][64]&lt;WatchableKey>**

### Aggregator

Aggregators can be used to compute aggregates for cell values.

Type: {key: [string][60], displayName: [string][60], shortDisplayName: [string][60], aggregate:
function (records: [Array][64]&lt;[Record][74]>, field: [Field][71]): any, aggregateToString:
function (records: [Array][64]&lt;[Record][74]>, field: [Field][71]): [string][60]}

#### Properties

-   `key` **[string][60]**
-   `displayName` **[string][60]**
-   `shortDisplayName` **[string][60]**
-   `aggregate` **function (records: [Array][64]&lt;[Record][74]>, field: [Field][71]): any**
-   `aggregateToString` **function (records: [Array][64]&lt;[Record][74]>, field: [Field][71]):
    [string][60]**

#### Examples

```javascript
// To get a list of aggregators supported for a specific field:
const fieldAggregators = myField.availableAggregators;

// To compute the total attachment size of an attachment field:
import {aggregators} from '@airtable/blocks/models';
const aggregator = aggregators.totalAttachmentSize;
const value = aggregator.aggregate(myRecords, myAttachmentField);
const valueAsString = aggregate.aggregateToString(myRecords, myAttachmentField);
```

### Base

**Extends AbstractModel**

Model class representing a base.

#### Examples

```javascript
import {base} from 'airtable-blocks';
```

#### activeCollaborators

The users who have access to this base.

Type: [Array][64]&lt;CollaboratorData>

Returns **[Array][64]&lt;CollaboratorData>**

#### currentUser

The current user, or `null` if the block is running in a publicly shared base.

Type: (CollaboratorData | null)

Returns **(CollaboratorData | null)**

#### getCollaboratorByIdIfExists

Returns the user matching the given ID, or `null` if that user does not exist or does not have
access to this base.

##### Parameters

-   `collaboratorId` **UserId**

Returns **(CollaboratorData | null)**

#### getTableByIdIfExists

Returns the table matching the given ID, or `null` if that table does not exist in this base.

##### Parameters

-   `tableId` **[string][60]**

Returns **([Table][75] | null)**

#### getTableByNameIfExists

Returns the table matching the given name, or `null` if no table exists with that name in this base.

##### Parameters

-   `tableName` **[string][60]**

Returns **([Table][75] | null)**

#### name

The name of the base.

Type: [string][60]

Returns **[string][60]**

#### permissionLevel

The current user's permission level.

The value of this should not be consumed and will be deprecated. To know whether a user can perform
an action, use the more specific `can` method.

Can be watched to know when the user's permission level changes. Usually, you'll want to watch this
in your root component and re-render your whole block when the permission level changes.

Type: [string][60]

##### Examples

```javascript
if (globalConfig.canSet('foo')) {
    globalConfig.set('foo', 'bar');
}
```

```javascript
if (record.canSetCellValue('Name', 'Chair')) {
    record.setCellValue('Name', 'Chair');
}
```

Returns **[string][60]**

#### tables

The tables in this base. Can be watched to know when tables are created, deleted, or reordered in
the base.

Type: [Array][64]&lt;[Table][75]>

Returns **[Array][64]&lt;[Table][75]>**

### Cursor

**Extends AbstractModelWithAsyncData**

Contains information about the state of the user's current interactions in Airtable

#### Parameters

-   `baseData` **BaseData**
-   `airtableInterface` **AirtableInterface**

#### Examples

```javascript
import {cursor} from '@airtable/blocks';
```

#### activeTableId

Returns the currently active table ID. Can return null when the active table has changed and is not
yet loaded.

Type: (TableId | null)

Returns **(TableId | null)**

#### activeViewId

Returns the currently active view ID. This will always be a view belonging to `activeTableId`.
Returns `null` when the active view has changed and is not yet loaded.

Type: (ViewId | null)

Returns **(ViewId | null)**

### Field

**Extends AbstractModel**

Model class representing a field in a table.

#### Parameters

-   `baseData` **BaseData**
-   `parentTable` **[Table][75]**
-   `fieldId` **[string][60]**

#### availableAggregators

Type: [Array][64]&lt;[Aggregator][76]>

Returns **[Array][64]&lt;[Aggregator][76]>**

#### canSetCellValues

Use this to check if the current user has permission to update the cell values in this field before
calling `record.setCellValues` or `table.setCellValues`.

Returns **[boolean][65]**

#### convertStringToCellValue

Given a string, will attempt to parse it and return a valid cell value for the field's current
config.

##### Parameters

-   `string` **[string][60]**

Returns **any**

#### isAggregatorAvailable

##### Parameters

-   `aggregator` **([Aggregator][76] \| [string][60])**

Returns **[boolean][65]**

#### isComputed

Type: [boolean][65]

Returns **[boolean][65]**

#### isPrimaryField

Every table has exactly one primary field. True if this field is its parent table's primary field.

Type: [boolean][65]

Returns **[boolean][65]**

#### name

Type: [string][60]

Returns **[string][60]**

#### options

Type: ({} | null)

Returns **({} | null)**

#### parentTable

Type: [Table][75]

Returns **[Table][75]**

#### type

Type: [string][60]

Returns **[string][60]**

### QueryResult

#### QueryResult

**Extends AbstractModelWithAsyncData**

##### Parameters

-   `recordStore` **RecordStore**
-   `normalizedOpts` **NormalizedQueryResultOpts**
-   `baseData` **BaseData**

##### fields

The fields that were used to create this QueryResult. Null if fields were not specified, which means
the QueryResult will load all fields in the table.

Type: ([Array][64]&lt;[Field][71]> | null)

Returns **([Array][64]&lt;[Field][71]> | null)**

##### parentTable

The table that records in this QueryResult are part of

Type: [Table][75]

Returns **[Table][75]**

##### recordIds

The record IDs in this QueryResult. Throws if data is not loaded yet.

Type: [Array][64]&lt;RecordId>

Returns **[Array][64]&lt;RecordId>**

##### records

The records in this QueryResult. Throws if data is not loaded yet.

Type: [Array][64]&lt;[Record][74]>

Returns **[Array][64]&lt;[Record][74]>**

#### TableOrViewQueryResult

**Extends QueryResult**

Represents a set of records directly from a view or table.

Do not instantiate. You can get instances of this class by calling `table.selectRecords` or
`view.selectRecords`.

##### Parameters

-   `sourceModel` **([Table][75] \| [View][72])**
-   `recordStore` **RecordStore**
-   `opts` **QueryResultOpts?**

##### fields

The fields that were used to create this QueryResult. Null if fields were not specified, which means
the QueryResult will load all fields in the table.

Type: ([Array][64]&lt;[Field][71]> | null)

Returns **([Array][64]&lt;[Field][71]> | null)**

##### parentTable

Type: [Table][75]

Returns **[Table][75]**

##### parentView

The view that was used to obtain this QueryResult by calling `view.selectRecords`. Null if the
QueryResult was obtained by calling `table.selectRecords`.

Type: ([View][72] | null)

Returns **([View][72] | null)**

##### recordIds

The record IDs in this QueryResult. Throws if data is not loaded yet.

Type: [Array][64]&lt;[string][60]>

Returns **[Array][64]&lt;[string][60]>**

#### LinkedRecordsQueryResult

**Extends QueryResult**

Represents a set of records from a LinkedRecord cell value.

Do not instantiate. You can get instances of this class by calling
`record.getLinkedRecordsFromCell`.

##### Parameters

-   `record` **[Record][74]**
-   `field` **[Field][71]**
-   `opts` **QueryResultOpts**

##### fields

The fields that were used to create this LinkedRecordsQueryResult.

Type: ([Array][64]&lt;[Field][71]> | null)

Returns **([Array][64]&lt;[Field][71]> | null)**

##### isValid

Is the query result currently valid? This value always starts as 'true', but can become false if the
field config changes to link to a different table or a type other than MULTIPLE_RECORD_LINKS. Once
`isValid` has become false, it will never become true again. Many fields will throw on attempting to
access them, and watches will no longer fire.

Type: [boolean][65]

Returns **[boolean][65]**

##### parentTable

The table that the records in the QueryResult are a part of

Type: [Table][75]

Returns **[Table][75]**

##### recordIds

Ordered array of all the linked record ids. Watchable.

Type: [Array][64]&lt;[string][60]>

Returns **[Array][64]&lt;[string][60]>**

##### records

Ordered array of all the linked records. Watchable.

Type: [Array][64]&lt;[Record][74]>

Returns **[Array][64]&lt;[Record][74]>**

### Record

**Extends AbstractModel**

Model class representing a record in a table.

Do not instantiate. To create a new record, use `table.createRecord`.

#### Parameters

-   `baseData` **BaseData**
-   `parentRecordStore` **RecordStore**
-   `parentTable` **[Table][75]**
-   `recordId` **[string][60]**

#### canDelete

Returns **[boolean][65]**

#### canSetCellValue

Use this to check if the current user has permission to update a specific cell value before calling
`setCellValue`.

##### Parameters

-   `fieldOrFieldIdOrFieldName` **([Field][71] \| [string][60])**
-   `publicCellValue` **any**

#### canSetCellValues

Use this to check if the current user has permission to update a set of cell values before calling
`setCellValues`.

##### Parameters

-   `cellValuesByFieldIdOrFieldName` **RecordDef**

Returns **[boolean][65]**

#### commentCount

Type: [number][67]

Returns **[number][67]**

#### createdTime

Type: [Date][77]

Returns **[Date][77]**

#### delete

Returns **AirtableWriteAction&lt;void, {}>**

#### getAttachmentClientUrlFromCellValueUrl

Call this method with an attachment ID and URL to get back a URL that is suitable for rendering on
the current client. The URL that is returned will only work for the current user.

##### Parameters

-   `attachmentId` **[string][60]**
-   `attachmentUrl` **[string][60]**

Returns **[string][60]**

#### getCellValue

##### Parameters

-   `fieldOrFieldIdOrFieldName` **([Field][71] \| [string][60])**

Returns **any**

#### getCellValueAsString

##### Parameters

-   `fieldOrFieldIdOrFieldName` **([Field][71] \| [string][60])**

Returns **[string][60]**

#### getColorHexInView

Get a CSS hex string for this record in the specified view, or null if no color is available. Watch
with the 'colorInView:\${ViewId}' key

##### Parameters

-   `viewOrViewIdOrViewName` **([View][72] \| [string][60])**

Returns **([string][60] | null)**

#### getColorInView

Get the color name for this record in the specified view, or null if no color is available. Watch
with the 'colorInView:\${ViewId}' key.

##### Parameters

-   `viewOrViewIdOrViewName` **([View][72] \| [string][60])**

Returns **([Color][78] | null)**

#### parentTable

Type: [Table][75]

Returns **[Table][75]**

#### primaryCellValue

Type: any

Returns **any**

#### primaryCellValueAsString

Type: [string][60]

Returns **[string][60]**

#### selectLinkedRecordsFromCell

##### Parameters

-   `fieldOrFieldIdOrFieldName` **([Field][71] \| [string][60])**
-   `opts` **QueryResultOpts** (optional, default `{}`)

Returns **[LinkedRecordsQueryResult][79]**

#### setCellValue

Use `canSetCellValue` to check if the current user has permission to update a specific cell value
before calling. Will throw if the user does not have permission.

##### Parameters

-   `fieldOrFieldIdOrFieldName` **([Field][71] \| [string][60])**
-   `publicCellValue` **any**

Returns **AirtableWriteAction&lt;void, {}>**

#### setCellValues

Use `canSetCellValues` to check if the current user has permission to update the cell values before
calling. Will throw if the user does not have permission.

##### Parameters

-   `cellValuesByFieldIdOrFieldName` **RecordDef**

Returns **AirtableWriteAction&lt;void, {}>**

#### url

Returns the URL for this record.

Type: [string][60]

Returns **[string][60]**

### Table

**Extends AbstractModel**

Model class representing a table in the base.

#### Parameters

-   `baseData` **BaseData**
-   `parentBase` **[Base][57]**
-   `recordStore` **RecordStore**
-   `tableId` **[string][60]**
-   `airtableInterface` **AirtableInterface**

#### canCreateRecord

##### Parameters

-   `cellValuesByFieldIdOrFieldName` **RecordDef?**

Returns **[boolean][65]**

#### canCreateRecords

##### Parameters

-   `recordDefsOrNumberOfRecords` **([Array][64]&lt;RecordDef> | [number][67])**

Returns **[boolean][65]**

#### canDeleteRecord

##### Parameters

-   `record` **[Record][74]**

#### canDeleteRecords

##### Parameters

-   `records` **[Array][64]&lt;[Record][74]>**

#### canSetCellValues

##### Parameters

-   `cellValuesByRecordIdThenFieldIdOrFieldName` **{}**

Returns **[boolean][65]**

#### createRecord

##### Parameters

-   `cellValuesByFieldIdOrFieldName` **RecordDef?**

Returns **AirtableWriteAction&lt;void, {record: [Record][74]}>**

#### createRecords

##### Parameters

-   `recordDefsOrNumberOfRecords` **([Array][64]&lt;RecordDef> | [number][67])**

Returns **AirtableWriteAction&lt;void, {records: [Array][64]&lt;[Record][74]>}>**

#### deleteRecord

##### Parameters

-   `record` **[Record][74]**

Returns **AirtableWriteAction&lt;void, {}>**

#### deleteRecords

##### Parameters

-   `records` **[Array][64]&lt;[Record][74]>**

Returns **AirtableWriteAction&lt;void, {}>**

#### fields

The fields in this table. The order is arbitrary, since fields are only ordered in the context of a
specific view.

Can be watched to know when fields are created or deleted.

Type: [Array][64]&lt;[Field][71]>

Returns **[Array][64]&lt;[Field][71]>**

#### getFieldByIdIfExists

##### Parameters

-   `fieldId` **FieldId**

Returns **([Field][71] | null)**

#### getFieldByNameIfExists

##### Parameters

-   `fieldName` **[string][60]**

Returns **([Field][71] | null)**

#### getFirstViewOfType

Returns the first view in the table where the type is one of `allowedViewTypes`. If a
`preferredViewOrViewId` is supplied and that view exists & has the correct type, that view will be
returned before checking the other views in the table.

##### Parameters

-   `allowedViewTypes` **([Array][64]&lt;ViewType> | ViewType)**
-   `preferredViewOrViewId` **([View][72] | ViewId | null)?**

Returns **([View][72] | null)**

#### getViewByIdIfExists

##### Parameters

-   `viewId` **ViewId**

Returns **([View][72] | null)**

#### getViewByNameIfExists

##### Parameters

-   `viewName` **[string][60]**

Returns **([View][72] | null)**

#### name

The table's name. Can be watched.

Type: [string][60]

Returns **[string][60]**

#### parentBase

Type: [Base][57]

Returns **[Base][57]**

#### primaryField

Every table has exactly one primary field. The primary field of a table will not change.

Type: [Field][71]

Returns **[Field][71]**

#### recordLimit

Maximum number of records that the table can contain

Type: [number][67]

Returns **[number][67]**

#### selectRecords

##### Parameters

-   `opts` **QueryResultOpts?**

Returns **[TableOrViewQueryResult][80]**

#### setCellValues

##### Parameters

-   `cellValuesByRecordIdThenFieldIdOrFieldName` **{}**

Returns **AirtableWriteAction&lt;void, {}>**

#### url

Type: [string][60]

Returns **[string][60]**

#### views

The views in the table. Can be watched to know when views are created, deleted, or reordered.

Type: [Array][64]&lt;[View][72]>

Returns **[Array][64]&lt;[View][72]>**

### View

**Extends AbstractModel**

A class that represents an Airtable view. Every [Table][20] has one or more views.

#### allFields

Type: [Array][64]&lt;[Field][71]>

##### Examples

```javascript
console.log(myView.allFields);
// => [Field {...}, Field {...}, ...]
```

Returns **[Array][64]&lt;[Field][71]>** All the fields in the table, including fields that are
hidden in this view. Can be watched to know when fields are created, deleted, or reordered.

#### name

Type: [string][60]

##### Examples

```javascript
console.log(myView.name);
// => 'Grid view'
```

Returns **[string][60]** The name of the view. Can be watched.

#### parentTable

Type: [Table][75]

##### Examples

```javascript
const view = myTable.getViewByName('Grid View');
console.log(view.parentTable.id === myTable.id);
// => true
```

Returns **[Table][75]** The table that this view belongs to. Should never change because views
aren't moved between tables.

#### selectRecords

Select records from the view. Returns a query result.

##### Parameters

-   `opts` **QueryResultOpts?** Options for the query, such as sorts and fields. (optional, default
    `{}`)

##### Examples

```javascript
import {UI} from '@airtable/blocks';
import React from 'react';

function TodoList() {
    const base = UI.useBase();
    const table = base.getTableByName('Tasks');
    const view = table.getViewByName('Grid view');

    const queryResult = view.selectRecords();
    const records = UI.useRecords(queryResult);

    return (
        <ul>
            {records.map(record => (
                <li key={record.id}>{record.primaryCellValueAsString || 'Unnamed record'}</li>
            ))}
        </ul>
    );
}
```

Returns **[TableOrViewQueryResult][80]** A query result.

#### type

Type: ViewType

##### Examples

```javascript
console.log(myView.type);
// => 'kanban'
```

Returns **ViewType** The type of the view, such as Grid, Calendar, or Kanban. Should never change
because view types cannot be modified.

#### url

Type: [string][60]

##### Examples

```javascript
console.log(myView.url);
// => https://airtable.com/tblxxxxxxxxxxxxxx/viwxxxxxxxxxxxxxx
```

Returns **[string][60]** The URL for the view. You can visit this URL in the browser to be taken to
the view in the Airtable UI.

#### visibleFields

Type: [Array][64]&lt;[Field][71]>

##### Examples

```javascript
console.log(myView.visibleFields);
// => [Field {...}, Field {...}, ...]
```

Returns **[Array][64]&lt;[Field][71]>** The fields that are visible in this view. Can be watched to
know when fields are created, deleted, hidden, shown, or reordered.

### Watchable

#### unwatch

Stop watching the given key or keys. Should be called with the same arguments that were given to
`watch`.

Will log a warning if the keys given are invalid.

##### Parameters

-   `keys` **(WatchableKey | [Array][64]&lt;WatchableKey>)**
-   `callback` **function (model: this, key: WatchableKey): any**
-   `context` **[Object][68]??**

Returns **[Array][64]&lt;WatchableKey>**

#### watch

Start watching the given key or keys. The callback will be called when the value changes. Every call
to `watch` should have a matching call to `unwatch`.

Will log a warning if the keys given are invalid.

##### Parameters

-   `keys` **(WatchableKey | [Array][64]&lt;WatchableKey>)**
-   `callback` **function (model: this, key: WatchableKey): any**
-   `context` **[Object][68]??**

Returns **[Array][64]&lt;WatchableKey>**

## @airtable/blocks/ui

### CSS & external scripts

#### loadCSSFromString

Injects CSS from a string into the page.

##### Parameters

-   `css` **[string][60]** {string}

##### Examples

```javascript
import {loadCSSFromString} from '@airtable/blocks/ui';
loadCSSFromString('body { background: red; }');
```

Returns **[HTMLStyleElement][81]** the style tag inserted into the page.

#### loadCSSFromURLAsync

Injects CSS from a remote URL.

##### Parameters

-   `url` **[string][60]** {string}

##### Examples

```javascript
import {loadScriptFromURLAsync} from '@airtable/blocks/ui';
loadCSSFromURLAsync('https://example.com/style.css');
```

Returns **[Promise][82]&lt;[HTMLLinkElement][83]>** a Promise that resolves to the style tag
inserted into the page.

#### loadScriptFromURLAsync

Injects Javascript from a remote URL.

##### Parameters

-   `url` **[string][60]** {string}

##### Examples

```javascript
import {loadScriptFromURLAsync} from '@airtable/blocks/ui';
loadScriptFromURLAsync('https://example.com/script.js');
```

Returns **[Promise][82]&lt;[HTMLScriptElement][84]>** a Promise that resolves to the script tag
inserted into the page.

### Color utilities

#### Color

A value from the [colors][85] enum

Type: \$Values&lt;any>

#### RGB

Type: {r: [number][67], g: [number][67], b: [number][67]}

##### Properties

-   `r` **[number][67]**
-   `g` **[number][67]**
-   `b` **[number][67]**

#### colorUtils

Utilities for working with [Color][86] names from the [colors][85] enum.

##### getHexForColor

Given a [Color][86], return the hex color value for that color, or null if the value isn't a
[Color][86]

###### Parameters

-   `colorString` {Color}

###### Examples

```javascript
import {colorUtils, colors} from '@airtable/blocks/ui';

colorUtils.getHexForColor(colors.RED);
// => '#ef3061'

colorUtils.getHexForColor('uncomfortable beige');
// => null
```

Returns **([string][60] | null)**

##### getRgbForColor

Given a [Color][86], return an [RGB][87] object representing it, or null if the value isn't a
[Color][86]

###### Parameters

-   `colorString` {Color}

###### Examples

```javascript
import {colorUtils, colors} from '@airtable/blocks/ui';

colorUtils.getRgbForColor(colors.PURPLE_DARK_1);
// => {r: 107, g: 28, b: 176}

colorUtils.getRgbForColor('disgruntled pink');
// => null
```

Returns **([RGB][88] | null)**

##### shouldUseLightTextOnColor

Given a [Color][86], returns true or false to indicate whether that color should have light text on
top of it when used as a background color.

###### Parameters

-   `colorString` **[string][60]** {Color}

###### Examples

```javascript
import {colorUtils, colors} from '@airtable/blocks/ui';

colorUtils.shouldUseLightTextOnColor(colors.PINK_LIGHT_1);
// => false

colorUtils.shouldUseLightTextOnColor(colors.PINK_DARK_1);
// => true
```

Returns **[boolean][65]** boolean

#### colors

An enum of color names

##### colors.BLUE

##### colors.BLUE_BRIGHT

##### colors.BLUE_DARK_1

##### colors.BLUE_LIGHT_1

##### colors.BLUE_LIGHT_2

##### colors.CYAN

##### colors.CYAN_BRIGHT

##### colors.CYAN_DARK_1

##### colors.CYAN_LIGHT_1

##### colors.CYAN_LIGHT_2

##### colors.GRAY

##### colors.GRAY_BRIGHT

##### colors.GRAY_DARK_1

##### colors.GRAY_LIGHT_1

##### colors.GRAY_LIGHT_2

##### colors.GREEN

##### colors.GREEN_BRIGHT

##### colors.GREEN_DARK_1

##### colors.GREEN_LIGHT_1

##### colors.GREEN_LIGHT_2

##### colors.ORANGE

##### colors.ORANGE_BRIGHT

##### colors.ORANGE_DARK_1

##### colors.ORANGE_LIGHT_1

##### colors.ORANGE_LIGHT_2

##### colors.PINK

##### colors.PINK_BRIGHT

##### colors.PINK_DARK_1

##### colors.PINK_LIGHT_1

##### colors.PINK_LIGHT_2

##### colors.PURPLE

##### colors.PURPLE_BRIGHT

##### colors.PURPLE_DARK_1

##### colors.PURPLE_LIGHT_1

##### colors.PURPLE_LIGHT_2

##### colors.RED

##### colors.RED_BRIGHT

##### colors.RED_DARK_1

##### colors.RED_LIGHT_1

##### colors.RED_LIGHT_2

##### colors.TEAL

##### colors.TEAL_BRIGHT

##### colors.TEAL_DARK_1

##### colors.TEAL_LIGHT_1

##### colors.TEAL_LIGHT_2

##### colors.YELLOW

##### colors.YELLOW_BRIGHT

##### colors.YELLOW_DARK_1

##### colors.YELLOW_LIGHT_1

##### colors.YELLOW_LIGHT_2

### React hooks for working with Airtable data

React hooks are a new feature in React 16.8. They allow you to use state and other React features
without writing a class, and form the core of how you can connect React components to data from your
Airtable base when writing Blocks. If you've never used hooks before, don't worry - the React team
[has some great resources][89] for learning about them. You can use hooks with class components too
with the help of the [withHooks][90] higher-order component.

> **Important Note:** right now, most of these hooks cause your component to re-render, but return
> the same mutable instance of the underlying model. This means that it's often not safe to use
> these hooks with React.memo or React.PureComponent. This will change in a future release of
> @airtable/blocks. If you're not sure what this means or if it effects you, don't worry! Just avoid
> React.memo & React.PureComponent and you'll be fine.

#### useBase

A hook for connecting a React component to your Base's schema. This returns a [Base][15] instance
and will re-render your component whenever the base's schema changes. That means any change to your
base like tables being added or removed, fields getting renamed, etc. It excludes any change to the
actual records in the base.

useBase should meet most of your needs for working with Base schema. If you need more granular
control of when your component updates or want to do anything other than re-render, the lower level
[useWatchable][91] hook might help.

##### Examples

```javascript
import {useBase} from '@airtable/blocks/ui';

// renders a list of tables and automatically updates
function TableList() {
    const base = useBase();

    const tables = base.tables.map(table => {
        return <li key={table.id}>{table.name}</li>;
    });

    return <ul>{tables}</ul>;
}
```

Returns **[Base][57]** Base

#### useRecords

A hook for working with all the records (including their colors and cell values) in a particular
query result. Automatically handles loading data in the query result and updating your component
when the underlying data changes.

This hook re-renders when any data concerning the records changing - that's useful, but can cause
re-renders quite often, meaning [useRecordIds][92] or [useRecordById][93] could be more appropriate
depending on your use-case.

##### Parameters

-   `queryResult` **([QueryResult][94] | null)** the query result you want the records from

##### Examples

```javascript
import {useRecords, useBase} from '@airtable/blocks';

function RecordList() {
    // get a query result for the records in the first table
    const base = useBase();
    const table = base.tables[0];
    const queryResult = table.selectRecords();

    // grab all the records from that query result
    const records = useRecords(queryResult);

    // render a list of records:
    return (
        <ul>
            {records.map(record => {
                return <li key={record.id}>{record.primaryCellValueAsString}</li>;
            })}
        </ul>
    );
}
```

Returns **([Array][64]&lt;[Record][74]> | null)** the records in the query result, or null if no
query result was passed in.

#### useRecordIds

A hook for working with the set of record IDs in a particular query result. Automatically handles
loading data in the query result and updating your component when the underlying data changes.

This hook doesn't re-render when the data _inside_ the records changes - only when records are
added, removed, or re-ordered.

##### Parameters

-   `queryResult` **[QueryResult][94]** the query result you want the record ids from

##### Examples

```javascript
import {useRecordIds, useBase} from '@airtable/blocks/ui';

function RecordCount() {
    // get a query result for the records in the first table
    const base = useBase();
    const table = base.tables[0];
    const queryResult = table.selectRecords();

    // grab all the record ids from that QueryResult
    const recordIds = useRecordIds(queryResult);

    // return the count
    return (
        <span>
            record count in {table.name}: {recordIds.length}
        </span>
    );
}
```

Returns **([Array][64]&lt;RecordId> | null)** the array of record IDs in the query result, or null
if no query result was passed in.

#### useRecordById

A hook for working with a single record from a query result. Automatically handles loading data in
the query result and updating your component when the records cell values etc. change.

Often used with [useRecordIds][92] to render a list of records where each list item only updates
when the specific record it concerns changes.

##### Parameters

-   `queryResult` **([QueryResult][94] | null)** the query result you want a record from
-   `recordId` **RecordId** the ID of the record you want from the query result

##### Examples

```javascript
import {useRecordById, useRecordIds, useBase} from '@airtable/blocks';

// this component concerns a single record - it only updates when that specific record updates
function RecordListItem({queryResult, recordId}) {
    const record = useRecord(queryResult, recordId);
    return <li>{record.primaryCellValueAsString}</li>;
}

// this component renders a list of records, but doesn't update when their cell values change -
// that's left up to RecordListItem
function RecordList() {
    // get a query result for the records in the first table
    const base = useBase();
    const table = base.tables[0];
    const queryResult = table.selectRecords();

    // grab all the record ids from that query result
    const recordIds = useRecordIds(queryResult);

    // render a list of records:
    return (
        <ul>
            {recordsIds.map(recordId => {
                return (
                    <RecordListItem key={recordId} recordId={recordId} queryResult={queryResult} />
                );
            })}
        </ul>
    );
}
```

Returns **([Record][74] | null)** the record, or null if no query result was passed in or no record
with that ID exists in the query result

#### useViewport

Returns the current [Viewport][5] object and updates whenever the viewport size, constraints, or
fullscreen status changes.

##### Examples

```javascript
import {useViewport} from '@airtable/blocks/ui';

function ViewportSize() {
    const viewport = useViewport();

    const toggleFullscreen = () => {
        if (viewport.isFullscreen) {
            viewport.exitFullscreen();
        } else {
            viewport.enterFullscreenIfPossible();
        }
    };

    return (
        <Fragment>
            <button onClick={toggleFullscreen}>Toggle fullscreen</button>
            viewport size: {viewport.size.width}x{viewport.size.height}
        </Fragment>
    );
}
```

Returns **[Viewport][63]** the current [Viewport][5]

#### useWatchable

A React hook for watching data in Airtable models like [Table][20] and [Record][19]. Each model has
several watchable keys that can be used with this hook to have your component automatically
re-render when data in the models changes. You can also provide an optional callback if you need to
do anything other than re-render when the data changes.

This is a low-level tool that you should only use when you specifically need it. There are more
convenient model-specific hooks available:

-   For [Base][15], [Table][20], [View][21] & [Field][17], use [useBase][95]
-   For [QueryResult][18] & [Record][19], use [useRecords][96], [useRecordIds][92], or
    [useRecordById][93]
-   For [Viewport][5], use [useViewport][97].

If you're writing a class component and still want to be able to use hooks, try [withHooks][90].

##### Parameters

-   `model` **[Watchable][98]** the model to watch
-   `keys` **[Array][64]&lt;[string][60]>** which keys we want to watch
-   `callback` **function (): any?** an optional callback to call when any of the watch keys change

##### Examples

```javascript
import {useWatchable} from '@airtable/blocks/ui';

function TableName({table}) {
    useWatchable(table, ['name']);
    return <span>The table name is {table.name}</span>;
}
```

```javascript
import {useWatchable} from '@airtable/blocks/ui';

function ActiveView({cursor}) {
    useWatchable(cursor, ['activeViewId'], () => {
        alert('active view changed!!!');
    });

    return <span>Active view id: {cursor.activeViewId}</span>;
}
```

#### useLoadable

When you're writing a block, not all of the data in your base is available to work with straight
away. We need to load it from Airtable first. This hook is a low-level tool for managing that. You
might not need to use it directly though - if you're working with a [QueryResult][18], try
[useRecords][96], [useRecordIds][92], or [useRecordById][93] first.

When you need to use a loadable mode, `useLoadable(theModel)` will make sure that the model is
loaded when your component mounts, and unloaded when your component unmounts. By default, you don't
need to worry about waiting for the data to load - the hook uses React Suspense to make sure the
rest of your component doesn't run until the data is loaded. Whilst the data is loading, the entire
block will show a loading indicator. If you want to change where that indicator shows or how it
looks, use [&lt;React.Suspense />][99] around the component that uses the hook.

If you need more control (for example, if you have two models you want to load at the same time
rather than one after the other), you can pass `{shouldSuspend: false}` as a second argument to the
hook. In that case though, you should check each model's `.isDataLoaded` property before trying to
use the data you loaded.

##### Parameters

-   `model` **([QueryResult][94] \| [Cursor][58] | null)** the model to load.
-   `options` **[object][68]?** Optional options to control how the hook works (optional, default
    `{}`)
    -   `options.shouldSuspend` **[boolean][65]** pass {shouldSuspend: false} to disable suspense
        mode. If suspense is disabled, you need to manually check model.isDataLoaded so you don't
        use your model before it's ready. (optional, default `true`)

##### Examples

```javascript
import {cursor} from '@airtable/blocks';
import {useLoadable, useWatchable} from '@airtable/blocks/ui';

function SelectedRecordIds() {
    // load selected records
    useLoadable(cursor);

    // re-render whenever the list of selected records changes
    useWatchable(cursor, ['selectedRecordIds']);

    // render the list of selected record ids
    return <div>Selected records: {cursor.selectedRecordIds.join(', ')}</div>;
}
```

```javascript
import {useLoadable} from '@airtable/blocks/ui';

function LoadTwoQueryResults({queryResultA, queryResultB}) {
    // we have two query results. load them without suspense so they can be loaded together,
    // rather than one after the other
    useLoadable(queryResultA, {shouldSuspend: false});
    useLoadable(queryResultB, {shouldSuspend: false});

    // manually check whether loading is finished or not before continuing
    if (!queryResultA.isDataLoaded || !queryResultB.isDataLoaded) {
        return <div>Loading...</div>;
    }

    // now, we can use the data
    return <SomeFancyComponent />;
}
```

#### withHooks

A helper method for working with react hooks in class-based components. It takes a React component
and wraps it, injecting values from hooks as additional props. `withHooks` uses
[React.forwardRef][100] to make sure that you can use refs with your wrapped component in exactly
the same way you would if you weren't using withHooks.

##### Parameters

-   `Component` **React.Component** The React component you want to inject hooks into
-   `getAdditionalPropsToInject` **[Function][73]** a function that takes props and returns more
    props to be injected into the wrapped component

##### Examples

```javascript
import React from 'react';
import {useRecords, withHooks} from '@airtable/blocks/ui';

// RecordList takes a list of records and renders it
class RecordList extends React.Component {
    render() {
        const records = this.props.records.map(record => {
            return <li key={record.id}>{record.primaryCellValueAsString}</li>;
        });

        return <ul>{records}</ul>;
    }
}

// using withHooks, we wrap up RecordList. It takes a queryResult prop, and injects a records
// prop from useRecords
const WrappedRecordList = withHooks(RecordList, ({queryResult}) => {
    const records = useRecords(queryResult);
    return {
        records: records,
    };
});

// when we use WrappedRecordList, we only need to pass in queryResult:
<WrappedRecordList queryResult={someQueryResult} />;
```

```javascript
import React from 'react';
import {Record, QueryResult} from '@airtable/blocks/models';
import {withHooks, useRecords} from '@airtable/blocks/ui';
// with flow, things are a little more complex: we need to provide some type annotations to
// indicate which props are injected:

type RequiredProps = {|
    queryResult: QueryResult,
|};

type InjectedProps = {|
    records: Array<Record>,
|};

type RecordListProps = {|
    ...RequiredProps,
    ...InjectedProps,
|};

class RecordList extends React.Component<RecordListProps> {
    // implementation is the same as the example above
}

// you need to annotate the return type as React.AbstractComponent. This takes two type args:
//   - the Config (usually just props) of the resulting component
//   - the instance type (what you get out of a ref) of the resulting component
const WrappedRecordList: React.AbstractComponent<RequiredProps, RecordList> = withHooks(
    RecordList,
    ({queryResult}) => {
        const records = useRecords(queryResult);
        return {
            records,
        };
    },
);

// when using a ref to the component, you can't refer to it as WrappedRecordList like a normal
// class component. Instead, you need to wrap it in React.ElementRef:
const ref: React.ElementRef<typeof WrappedRecordList> = getTheRefSomehow();
```

Returns **React.Component** the wrapped React component

### expandRecord

#### expandRecord

Expands the given record in the Airtable UI.

##### Parameters

-   `record` **[Record][74]** the record to expand
-   `opts` **[object][68]?** An optional options object.
    -   `opts.records` **[Array][64]&lt;[Record][74]>?** If `records` is provided, the list will be
        used to page through records from the expanded record dialog.

##### Examples

```javascript
import {expandRecord} from '@airtable/blocks/ui';
expandRecord(record1, {
    records: [record1, record2, record3],
});
```

#### ExpandRecordOpts

Type: {records: [Array][64]&lt;[Record][74]>?}

##### Properties

-   `records` **[Array][64]&lt;[Record][74]>?**

### expandRecordList

Expands a list of records in the Airtable UI

#### Parameters

-   `records` **[Array][64]&lt;[Record][74]>** the records to expand. Duplicate records will be
    removed.
-   `opts` **{fields: [Array][64]&lt;[Field][71]>?}?** An optional options object.
    -   `opts.fields` optionally include an array of fields to control which fields are shown in the
        record cards. The primary field will always be shown. Duplicate fields will be removed.

#### Examples

```javascript
import {expandRecordList} from '@airtable/blocks/ui';
expandRecordList([record1, record2, record3]);

expandRecordList([record1, record2], {
    fields: [field1, field2],
});
```

### expandRecordPickerAsync

Expands a list of records in the Airtable UI, and prompts the user to pick one. The selected record
is returned to the block, and the modal is automatically closed.

If the user dismisses the modal, or another one is opened before this one has been closed, it will
return null.

#### Parameters

-   `records` **[Array][64]&lt;[Record][74]>** the records the user can pick from. Duplicate records
    will be removed.
-   `opts` **{fields: [Array][64]&lt;[Field][71]>?, shouldAllowCreatingRecord: [boolean][65]?}?** An
    optional options object.
    -   `opts.fields` optionally include an array of fields to control which fields are shown in the
        record cards. The primary field will always be shown. Duplicate fields will be removed.
    -   `opts.shouldAllowCreatingRecord` set to true to allow the user to create an empty new
        record.

#### Examples

```javascript
import {expandRecordPickerAsync} from '@airtable/blocks/ui';

const recordA = await expandRecordPickerAsync([record1, record2, record3]);
if (recordA !== null) {
    alert(recordA.primaryCellValueAsString);
} else {
    alert('no record picked');
}

const recordB = await expandRecordPickerAsync([record1, record2], {
    fields: [field1, field2],
});
```

Returns **[Promise][82]&lt;(record | null)>** a Promise that resolves to the record chosen by the
user or null

### globalAlert

**Extends Watchable**

#### Examples

```javascript
import {globalAlert} from '@airtable/blocks/ui';
globalAlert.showReloadPrompt();
```

#### showReloadPrompt

### AutocompletePopover

#### AutocompletePopover

**Extends React.Component**

##### Parameters

-   `props` **[AutocompletePopoverProps][101]**

#### AutocompletePopoverProps

Type: {children:
React$Element&lt;any>, items: [Array][64]&lt;[AutocompleteItem][102]>, renderItem: function (item: [AutocompleteItem][102], isFocused: [boolean][65]): React$Element&lt;any>?,
filterItems: function (query: [string][60], items: [Array][64]&lt;[AutocompleteItem][102]>):
[Array][64]&lt;[AutocompleteItem][102]>?, onSelect: function ([AutocompleteItem][102]): void,
placeholder: [string][60]?, focusOnOpen: [boolean][65]?, className: [string][60]?, style:
[Object][68]?, placementX: PopoverPlacementX?, placementY: PopoverPlacementY?, placementOffsetX:
[number][67]?, placementOffsetY: [number][67]?, fitInWindowMode: FitInWindowMode?, isOpen:
[boolean][65]?, onClose: function (opts: {wasFromEscape: [boolean][65]}): void?}

##### Properties

-   `children` **React\$Element&lt;any>**
-   `items` **[Array][64]&lt;[AutocompleteItem][102]>**
-   `renderItem` **function (item: [AutocompleteItem][102], isFocused: [boolean][65]):
    React\$Element&lt;any>?**
-   `filterItems` **function (query: [string][60], items: [Array][64]&lt;[AutocompleteItem][102]>):
    [Array][64]&lt;[AutocompleteItem][102]>?**
-   `onSelect` **function ([AutocompleteItem][102]): void**
-   `placeholder` **[string][60]?**
-   `focusOnOpen` **[boolean][65]?**
-   `className` **[string][60]?**
-   `style` **[Object][68]?**
-   `placementX` **PopoverPlacementX?**
-   `placementY` **PopoverPlacementY?**
-   `placementOffsetX` **[number][67]?**
-   `placementOffsetY` **[number][67]?**
-   `fitInWindowMode` **FitInWindowMode?**
-   `isOpen` **[boolean][65]?**
-   `onClose` **function (opts: {wasFromEscape: [boolean][65]}): void?**

#### AutocompleteItem

Type: {value: [string][60], label: [string][60], aliases: [Array][64]&lt;[string][60]>?}

##### Properties

-   `value` **[string][60]**
-   `label` **[string][60]**
-   `aliases` **[Array][64]&lt;[string][60]>?**

### Button

#### Button

**Extends React.Component**

Clickable button component.

##### Parameters

-   `props` **[ButtonProps][103]**

##### Examples

```javascript
import UI from '@airtable/blocks/ui';

const button = (
    <UI.Button onClick={() => alert('Clicked!')} disabled={false} theme={Button.themes.BLUE}>
        Click here!
    </UI.Button>
);
```

#### ButtonProps

Type: [object][68]

##### Properties

-   `theme` **(Button.themes.RED | Button.themes.GREEN | Button.themes.BLUE | Button.themes.YELLOW |
    Button.themes.WHITE | Button.themes.GRAY | Button.themes.DARK | Button.themes.TRANSPARENT)?**
    The color theme for the button.
-   `className` **[string][60]?**
-   `disabled` **[boolean][65]?**
-   `children` **React.Node?**

### CellRenderer

#### CellRenderer

**Extends React.Component**

##### Parameters

-   `props` **[CellRendererProps][104]**

#### CellRendererProps

Type: {record: [Record][74]??, cellValue: any?, field: [Field][71], shouldWrap: [boolean][65]?,
className: [string][60]?, style: [Object][68]?}

##### Properties

-   `record` **[Record][74]??**
-   `cellValue` **any?**
-   `field` **[Field][71]**
-   `shouldWrap` **[boolean][65]?**
-   `className` **[string][60]?**
-   `style` **[Object][68]?**

### ChoiceToken

#### ChoiceToken

##### Parameters

-   `$0` **[ChoiceTokenProps][105]**
    -   `$0.choice`
    -   `$0.className`

#### ChoiceTokenProps

Type: {choice: {id: [string][60], name: [string][60], color: [string][60]?}, className:
[string][60]?}

##### Properties

-   `choice` **{id: [string][60], name: [string][60], color: [string][60]?}**
-   `className` **[string][60]?**

### CollaboratorToken

#### CollaboratorToken

##### Parameters

-   `props` **[CollaboratorTokenProps][106]**

#### CollaboratorTokenProps

Type: {collaborator: {id: [string][60]?, email: [string][60]?, name: [string][60]?, profilePicUrl:
[string][60]?, status: [string][60]?}, className: [string][60]?}

##### Properties

-   `collaborator` **{id: [string][60]?, email: [string][60]?, name: [string][60]?, profilePicUrl:
    [string][60]?, status: [string][60]?}**
-   `className` **[string][60]?**

### ColorPalette

#### ColorPalette

**Extends React.Component**

#### ColorPaletteProps

Type: {color: [string][60]?, allowedColors: [Array][64]&lt;[string][60]>, onChange: function
([string][60]): any?, squareSize: [number][67], squareMargin: [number][67], className: [string][60],
style: [Object][68], disabled: [boolean][65]?}

##### Properties

-   `color` **[string][60]?**
-   `allowedColors` **[Array][64]&lt;[string][60]>**
-   `onChange` **function ([string][60]): any?**
-   `squareSize` **[number][67]**
-   `squareMargin` **[number][67]**
-   `className` **[string][60]**
-   `style` **[Object][68]**
-   `disabled` **[boolean][65]?**

#### ColorPaletteSynced

**Extends React.Component**

#### ColorPaletteSyncedProps

Type: {globalConfigKey: [string][60], disabled: [boolean][65]?, onChange: function ([string][60]):
any?}

##### Properties

-   `globalConfigKey` **[string][60]**
-   `disabled` **[boolean][65]?**
-   `onChange` **function ([string][60]): any?**

### ConfirmationDialog

**Extends React.Component**

A styled modal dialog component that prompts the user to confirm or cancel an action. By default,
this component will focus the "Confirm" button on mount, so that pressing the Enter key will confirm
the action.

#### Parameters

-   `props` **ConfirmationDialogProps**

#### Examples

```javascript
import {Button, Dialog, ConfirmationDialog} from '@airtable/blocks/ui';
import React, {Fragment, useState} from 'react';

function Block() {
    const [isDialogOpen, setIsDialogOpen] = useState(false);
    return (
        <Fragment>
            <Button theme={Button.themes.BLUE} onClick={() => setIsDialogOpen(true)}>
                Open dialog
            </Button>
            {isDialogOpen && (
                <ConfirmationDialog
                    title="Are you sure?"
                    body="This action can't be undone."
                    onConfirm={() => {
                        alert('Confirmed.');
                        setIsDialogOpen(false);
                    }}
                    onCancel={() => setIsDialogOpen(false)}
                />
            )}
        </Fragment>
    );
}
```

#### ConfirmationDialogProps

Type: {title: [string][60], body: React.Node?, cancelButtonText: [string][60], confirmButtonText:
[string][60], isConfirmActionDangerous: [boolean][65], className: [string][60]?, style:
[Object][68]?, onCancel: function (): any, onConfirm: function (): any}

##### Properties

-   `title` **[string][60]**
-   `body` **React.Node?**
-   `cancelButtonText` **[string][60]**
-   `confirmButtonText` **[string][60]**
-   `isConfirmActionDangerous` **[boolean][65]**
-   `className` **[string][60]?**
-   `style` **[Object][68]?**
-   `onCancel` **function (): any**
-   `onConfirm` **function (): any**

### Dialog

#### Dialog

**Extends React.Component**

A styled modal dialog component.

##### Parameters

-   `props` **[DialogProps][107]**

##### Examples

```javascript
import {Button, Dialog} from '@airtable/blocks/ui';
import React, {Fragment, useState} from 'react';

function Block() {
    const [isDialogOpen, setIsDialogOpen] = useState(false);
    return (
        <Fragment>
            <Button theme={Button.themes.BLUE} onClick={() => setIsDialogOpen(true)}>
                Open dialog
            </Button>
            {isDialogOpen && (
                <Dialog onClose={() => setIsDialogOpen(false)}>
                    <Fragment>
                        <Dialog.CloseButton />
                        <h1
                            style={{
                                marginBottom: 8,
                                fontSize: 20,
                                fontWeight: 500,
                            }}
                        >
                            Dialog
                        </h1>
                        <p>This is the dialog content.</p>
                    </Fragment>
                </Dialog>
            )}
        </Fragment>
    );
}
```

#### DialogProps

Type: {onClose: function (): any, className: [string][60]?, style: [Object][68]?,
backgroundClassName: [string][60]?, backgroundStyle: [Object][68]?, children: React.Node}

##### Properties

-   `onClose` **function (): any**
-   `className` **[string][60]?**
-   `style` **[Object][68]?**
-   `backgroundClassName` **[string][60]?**
-   `backgroundStyle` **[Object][68]?**
-   `children` **React.Node**

#### Dialog.CloseButton

**Extends React.Component**

##### Parameters

-   `props` **DialogCloseButtonProps**

#### Dialog.CloseButtonProps

Type: {className: [string][60]?, style: [Object][68]?, tabIndex: [number][67]?, children:
React.Node?}

##### Properties

-   `className` **[string][60]?**
-   `style` **[Object][68]?**
-   `tabIndex` **[number][67]?**
-   `children` **React.Node?**

### FieldIcon

#### FieldIcon

##### Parameters

-   `props` **[FieldIconProps][108]**

#### FieldIconProps

Type: {field: [Field][71], size: [number][67]?, fillColor: [string][60]?, className: [string][60]?,
style: [Object][68]?, pathClassName: [string][60]?}

##### Properties

-   `field` **[Field][71]**
-   `size` **[number][67]?**
-   `fillColor` **[string][60]?**
-   `className` **[string][60]?**
-   `style` **[Object][68]?**
-   `pathClassName` **[string][60]?**

### FieldPicker

#### FieldPicker

**Extends React.Component**

##### Parameters

-   `props` **[FieldPickerProps][109]**

#### FieldPickerProps

Type: {table: [Table][75]?, field: [Field][71]?, shouldAllowPickingNone: [boolean][65]?, onChange:
function (fieldModel: ([Field][71] | null)): void?, allowedTypes: [Array][64]&lt;FieldType>?,
placeholder: [string][60]?, style: [Object][68]?, className: [string][60]?, disabled:
[boolean][65]?}

##### Properties

-   `table` **[Table][75]?**
-   `field` **[Field][71]?**
-   `shouldAllowPickingNone` **[boolean][65]?**
-   `onChange` **function (fieldModel: ([Field][71] | null)): void?**
-   `allowedTypes` **[Array][64]&lt;FieldType>?**
-   `placeholder` **[string][60]?**
-   `style` **[Object][68]?**
-   `className` **[string][60]?**
-   `disabled` **[boolean][65]?**

#### FieldPickerSynced

**Extends React.Component**

##### Parameters

-   `props` **[FieldPickerSyncedProps][110]**

#### FieldPickerSyncedProps

Type: {table: [Table][75]?, globalConfigKey: GlobalConfigKey, onChange: function (fieldModel:
([Field][71] | null)): void?, disabled: [boolean][65]?, shouldAllowPickingNone: [boolean][65]?,
allowedTypes: [Array][64]&lt;FieldType>?, placeholder: [string][60]?, style: [Object][68]?,
className: [string][60]?}

##### Properties

-   `table` **[Table][75]?**
-   `globalConfigKey` **GlobalConfigKey**
-   `onChange` **function (fieldModel: ([Field][71] | null)): void?**
-   `disabled` **[boolean][65]?**
-   `shouldAllowPickingNone` **[boolean][65]?**
-   `allowedTypes` **[Array][64]&lt;FieldType>?**
-   `placeholder` **[string][60]?**
-   `style` **[Object][68]?**
-   `className` **[string][60]?**

### Icon

#### Icon

##### Parameters

-   `$0` **[IconProps][111]**
    -   `$0.name`
    -   `$0.size` (optional, default `16`)
    -   `$0.fillColor`
    -   `$0.className`
    -   `$0.style`
    -   `$0.pathClassName`

#### IconProps

Type: {name: [string][60], size: [number][67]?, fillColor: [string][60]?, className: [string][60]?,
style: [Object][68]?, pathClassName: [string][60]?}

##### Properties

-   `name` **[string][60]**
-   `size` **[number][67]?**
-   `fillColor` **[string][60]?**
-   `className` **[string][60]?**
-   `style` **[Object][68]?**
-   `pathClassName` **[string][60]?**

### Input

#### Input

**Extends React.Component**

##### Parameters

-   `props` **[InputProps][112]**

#### InputProps

Type: {type: [string][60]?, placeholder: [string][60]?, onChange: function
(SyntheticInputEvent&lt;>): void?, style: [Object][68]?, className: [string][60]?, disabled:
[boolean][65]?, required: [boolean][65]?, spellCheck: [boolean][65]?, tabIndex: [number][67]?}

##### Properties

-   `type` **[string][60]?**
-   `placeholder` **[string][60]?**
-   `onChange` **function (SyntheticInputEvent&lt;>): void?**
-   `style` **[Object][68]?**
-   `className` **[string][60]?**
-   `disabled` **[boolean][65]?**
-   `required` **[boolean][65]?**
-   `spellCheck` **[boolean][65]?**
-   `tabIndex` **[number][67]?**

#### InputSynced

**Extends React.Component**

##### Parameters

-   `props` **[InputSyncedProps][113]**

#### InputSyncedProps

Type: {type: [string][60]?, globalConfigKey: GlobalConfigKey, placeholder: [string][60]?, onChange:
function (SyntheticInputEvent&lt;>): void?, style: [Object][68]?, className: [string][60]?,
disabled: [boolean][65]?, spellCheck: [boolean][65]?}

##### Properties

-   `type` **[string][60]?**
-   `globalConfigKey` **GlobalConfigKey**
-   `placeholder` **[string][60]?**
-   `onChange` **function (SyntheticInputEvent&lt;>): void?**
-   `style` **[Object][68]?**
-   `className` **[string][60]?**
-   `disabled` **[boolean][65]?**
-   `spellCheck` **[boolean][65]?**

### Link

A wrapper around the `<a>` tag that offers a few security benefits:

-   Limited XSS protection. If the `href` starts with `javascript:` or `data:`, `http://` will be
    prepended.
-   There is [reverse tabnabbing prevention][114]. If `target` is set, the `rel` attribute will be
    set to `noopener noreferrer`.

Developers should use `Link` instead of `a` when possible.

#### Parameters

-   `props` **LinkProps**

#### Examples

```javascript
import {UI} from '@airtable/blocks/ui';

function MyLinkComponent() {
    return <UI.Link href="https://example.com">Check out my homepage!</UI.Link>;
}
```

#### LinkProps

Type: {href: [string][60], target: [string][60]?, tabIndex: ([number][67] \| [string][60])?,
className: [string][60]?, style: [Object][68]?, children: React.Node}

##### Properties

-   `href` **[string][60]**
-   `target` **[string][60]?**
-   `tabIndex` **([number][67] \| [string][60])?**
-   `className` **[string][60]?**
-   `style` **[Object][68]?**
-   `children` **React.Node**

### Loader

#### Loader

##### Parameters

-   `$0` **[LoaderProps][115]**
    -   `$0.fillColor` (optional, default `'#888'`)
    -   `$0.scale` (optional, default `0.3`)

#### LoaderProps

Type: {fillColor: [string][60]?, scale: [number][67]?}

##### Properties

-   `fillColor` **[string][60]?**
-   `scale` **[number][67]?**

### Popover

#### Popover

**Extends React.Component**

##### Parameters

-   `props` **[PopoverProps][116]**

#### PopoverProps

Type: {children: React$Element&lt;any>, renderContent: function (): React$Element&lt;any>,
placementX: PopoverPlacementX, placementY: PopoverPlacementY, placementOffsetX: [number][67],
placementOffsetY: [number][67], fitInWindowMode: FitInWindowMode, onClose: function (): void?,
isOpen: [boolean][65], backgroundClassName: [string][60]?, backgroundStyle: [Object][68]?}

##### Properties

-   `children` **React\$Element&lt;any>**
-   `renderContent` **function (): React\$Element&lt;any>**
-   `placementX` **PopoverPlacementX**
-   `placementY` **PopoverPlacementY**
-   `placementOffsetX` **[number][67]**
-   `placementOffsetY` **[number][67]**
-   `fitInWindowMode` **FitInWindowMode**
-   `onClose` **function (): void?**
-   `isOpen` **[boolean][65]**
-   `backgroundClassName` **[string][60]?**
-   `backgroundStyle` **[Object][68]?**

#### Popover.fitInWindowModes

#### Popover.placements

### ProgressBar

#### ProgressBar

A progress bar.

##### Parameters

-   `props` **[ProgressBarProps][117]**

##### Examples

```javascript
import {UI} from '@airtable/blocks/ui';

function MyComponent() {
    return <ProgressBar progress={0.6} barColor="#ff9900" />;
}
```

#### ProgressBarProps

Type: [object][68]

##### Properties

-   `progress` **[number][67]** A number between 0 and 1. 0 is 0% complete, 0.5 is 50% complete, 1
    is 100% complete. If you include a number outside of the range, the value will be clamped to be
    inside of the range.
-   `barColor` **[string][60]?** A CSS color, such as `#ff9900`.
-   `backgroundColor` **[string][60]?** A CSS color, such as `#ff9900`.
-   `height` **[number][67]?** A height, in pixels.
-   `className` **[string][60]?** Extra `className`s to apply to the element, separated by spaces.
-   `style` **[object][68]?** Extra styles to apply to the progress bar.

### RadioSynced

#### RadioSynced

**Extends React.Component**

##### Parameters

-   `props` **[RadioSyncedProps][118]**

#### RadioSyncedProps

Type: {globalConfigKey: GlobalConfigKey, value: [string][60], style: [Object][68]?, className:
[string][60]?, disabled: [boolean][65]?}

##### Properties

-   `globalConfigKey` **GlobalConfigKey**
-   `value` **[string][60]**
-   `style` **[Object][68]?**
-   `className` **[string][60]?**
-   `disabled` **[boolean][65]?**

### RecordCard

#### RecordCard

**Extends React.Component**

##### Parameters

-   `props` **[RecordCardProps][119]**

#### RecordCardProps

Type: {record: ([Record][74] | RecordDef), fields: [Array][64]&lt;[Field][71]>?, view: [View][72]?,
attachmentCoverField: [Field][71]?, width: [number][67]?, height: [number][67]?, onClick:
[Function][73]?, getExpandRecordOptions: function ([Record][74]): [ExpandRecordOpts][120]?,
onMouseEnter: any?, onMouseLeave: any?, className: [string][60]?, style: [Object][68]?}

##### Properties

-   `record` **([Record][74] | RecordDef)**
-   `fields` **[Array][64]&lt;[Field][71]>?**
-   `view` **[View][72]?**
-   `attachmentCoverField` **[Field][71]?**
-   `width` **[number][67]?**
-   `height` **[number][67]?**
-   `onClick` **[Function][73]?**
-   `getExpandRecordOptions` **function ([Record][74]): [ExpandRecordOpts][120]?**
-   `onMouseEnter` **any?**
-   `onMouseLeave` **any?**
-   `className` **[string][60]?**
-   `style` **[Object][68]?**

### RecordCardList

#### RecordCardList

**Extends React.Component**

##### Parameters

-   `props` **[RecordCardListProps][121]**

#### RecordCardListProps

Type: {records: [Array][64]&lt;([Record][74] | RecordDef)>, onScroll: function ([Event][122]):
void?, onRecordClick: (null | function (record: ([Record][74] | RecordDef), index: [number][67]):
void)?, onRecordMouseEnter: function (record: ([Record][74] | RecordDef), index: [number][67]):
void?, onRecordMouseLeave: function (record: ([Record][74] | RecordDef), index: [number][67]):
void?, fields: [Array][64]&lt;[Field][71]>?, view: [View][72]?, attachmentCoverField: [Field][71]?,
className: [string][60]?, style: [Object][68]?}

##### Properties

-   `records` **[Array][64]&lt;([Record][74] | RecordDef)>**
-   `onScroll` **function ([Event][122]): void?**
-   `onRecordClick` **(null | function (record: ([Record][74] | RecordDef), index: [number][67]):
    void)?**
-   `onRecordMouseEnter` **function (record: ([Record][74] | RecordDef), index: [number][67]):
    void?**
-   `onRecordMouseLeave` **function (record: ([Record][74] | RecordDef), index: [number][67]):
    void?**
-   `fields` **[Array][64]&lt;[Field][71]>?**
-   `view` **[View][72]?**
-   `attachmentCoverField` **[Field][71]?**
-   `className` **[string][60]?**
-   `style` **[Object][68]?**

### Select

#### Select

**Extends React.Component**

##### Parameters

-   `props` **[SelectProps][123]**

#### SelectProps

Type: SelectAndSelectButtonsProps

#### SelectSynced

**Extends React.Component**

##### Parameters

-   `props` **[SelectSyncedProps][124]**

#### SelectSyncedProps

Type: SelectAndSelectButtonsSyncedProps

### SelectButtons

#### SelectButtons

**Extends React.Component**

#### SelectButtonsProps

Type: SelectAndSelectButtonsProps

#### SelectButtonsSynced

**Extends React.Component**

#### SelectButtonsSyncedProps

Type: SelectAndSelectButtonsSyncedProps

### TablePicker

#### TablePicker

**Extends React.Component**

##### Parameters

-   `props` **[TablePickerProps][125]**

#### TablePickerProps

Type: {table: [Table][75]?, shouldAllowPickingNone: [boolean][65]?, onChange: function (tableModel:
([Table][75] | null)): void?, placeholder: [string][60]?, style: [Object][68]?, className:
[string][60]?, disabled: [boolean][65]?}

##### Properties

-   `table` **[Table][75]?**
-   `shouldAllowPickingNone` **[boolean][65]?**
-   `onChange` **function (tableModel: ([Table][75] | null)): void?**
-   `placeholder` **[string][60]?**
-   `style` **[Object][68]?**
-   `className` **[string][60]?**
-   `disabled` **[boolean][65]?**

#### TablePickerSynced

**Extends React.Component**

##### Parameters

-   `props` **[TablePickerSyncedProps][126]**

#### TablePickerSyncedProps

Type: {globalConfigKey: GlobalConfigKey, onChange: function (tableModel: ([Table][75] | null)):
void?, disabled: [boolean][65]?, shouldAllowPickingNone: [boolean][65]?, placeholder: [string][60]?,
style: [Object][68]?, className: [string][60]?}

##### Properties

-   `globalConfigKey` **GlobalConfigKey**
-   `onChange` **function (tableModel: ([Table][75] | null)): void?**
-   `disabled` **[boolean][65]?**
-   `shouldAllowPickingNone` **[boolean][65]?**
-   `placeholder` **[string][60]?**
-   `style` **[Object][68]?**
-   `className` **[string][60]?**

### Toggle

#### Toggle

**Extends React.Component**

##### Parameters

-   `props` **[ToggleProps][127]**

#### ToggleProps

Type: {value: [boolean][65], label: React.Node?, theme: [string][60]?, onChange: function
([boolean][65]): void?, disabled: [boolean][65]?, className: [string][60]?, style: [Object][68]?,
tabIndex: [number][67]?}

##### Properties

-   `value` **[boolean][65]**
-   `label` **React.Node?**
-   `theme` **[string][60]?**
-   `onChange` **function ([boolean][65]): void?**
-   `disabled` **[boolean][65]?**
-   `className` **[string][60]?**
-   `style` **[Object][68]?**
-   `tabIndex` **[number][67]?**

#### ToggleSynced

**Extends React.Component**

##### Parameters

-   `props` **[ToggleSyncedProps][128]**

#### ToggleSyncedProps

Type: {globalConfigKey: GlobalConfigKey, label: React.Node?, theme: [string][60]?, onChange:
function ([boolean][65]): void?, disabled: [boolean][65]?, className: [string][60]?, style:
[Object][68]?, tabIndex: [number][67]?}

##### Properties

-   `globalConfigKey` **GlobalConfigKey**
-   `label` **React.Node?**
-   `theme` **[string][60]?**
-   `onChange` **function ([boolean][65]): void?**
-   `disabled` **[boolean][65]?**
-   `className` **[string][60]?**
-   `style` **[Object][68]?**
-   `tabIndex` **[number][67]?**

### Tooltip

#### Tooltip

**Extends React.Component**

##### Parameters

-   `props` **[TooltipProps][129]**

#### TooltipProps

Type: {children: React$Element&lt;any>, renderContent: function (): React$Element&lt;any>?, content:
([string][60] | function (): React\$Element&lt;any>)?, placementX: PopoverPlacementX?, placementY:
PopoverPlacementY?, placementOffsetX: [number][67]?, placementOffsetY: [number][67]?,
fitInWindowMode: FitInWindowMode?, shouldHideTooltipOnClick: [boolean][65]?, disabled:
[boolean][65]?, className: [string][60]?, style: [Object][68]?}

##### Properties

-   `children` **React\$Element&lt;any>**
-   `renderContent` **function (): React\$Element&lt;any>?**
-   `content` **([string][60] | function (): React\$Element&lt;any>)?**
-   `placementX` **PopoverPlacementX?**
-   `placementY` **PopoverPlacementY?**
-   `placementOffsetX` **[number][67]?**
-   `placementOffsetY` **[number][67]?**
-   `fitInWindowMode` **FitInWindowMode?**
-   `shouldHideTooltipOnClick` **[boolean][65]?**
-   `disabled` **[boolean][65]?**
-   `className` **[string][60]?**
-   `style` **[Object][68]?**

### ViewPicker

#### ViewPicker

**Extends React.Component**

##### Parameters

-   `props` **[ViewPickerProps][130]**

#### ViewPickerProps

Type: {table: [Table][75]?, view: [View][72]?, shouldAllowPickingNone: [boolean][65]?, onChange:
function (viewModel: ([View][72] | null)): void?, allowedTypes: [Array][64]&lt;ViewType>?,
placeholder: [string][60]?, style: [Object][68]?, className: [string][60]?, disabled:
[boolean][65]?}

##### Properties

-   `table` **[Table][75]?**
-   `view` **[View][72]?**
-   `shouldAllowPickingNone` **[boolean][65]?**
-   `onChange` **function (viewModel: ([View][72] | null)): void?**
-   `allowedTypes` **[Array][64]&lt;ViewType>?**
-   `placeholder` **[string][60]?**
-   `style` **[Object][68]?**
-   `className` **[string][60]?**
-   `disabled` **[boolean][65]?**

#### ViewPickerSynced

**Extends React.Component**

##### Parameters

-   `props` **[ViewPickerSyncedProps][131]**

#### ViewPickerSyncedProps

Type: {table: [Table][75]?, globalConfigKey: GlobalConfigKey, onChange: function (viewModel:
([View][72] | null)): void?, disabled: [boolean][65]?, shouldAllowPickingNone: [boolean][65]?,
allowedTypes: [Array][64]&lt;ViewType>?, placeholder: [string][60]?, style: [Object][68]?,
className: [string][60]?}

##### Properties

-   `table` **[Table][75]?**
-   `globalConfigKey` **GlobalConfigKey**
-   `onChange` **function (viewModel: ([View][72] | null)): void?**
-   `disabled` **[boolean][65]?**
-   `shouldAllowPickingNone` **[boolean][65]?**
-   `allowedTypes` **[Array][64]&lt;ViewType>?**
-   `placeholder` **[string][60]?**
-   `style` **[Object][68]?**
-   `className` **[string][60]?**

### ViewportConstraint

#### ViewportConstraint

**Extends React.Component**

-   **See: sdk.viewport**

ViewportConstraint - when mounted, applies constraints to the viewport.

##### Examples

```javascript
import {ViewportConstraint} from '@airtable/blocks/ui';
<ViewportConstraint minSize={{width: 400}} />;
```

```javascript
import {ViewportConstraint} from '@airtable/blocks/ui';
<ViewportConstraint maxFullScreenSize={{width: 600, height: 400}}>
    <div>I need a max fullscreen size!</div>
</ViewportConstraint>;
```

#### ViewportConstraintProps

Type: {minSize: [ViewportSizeConstraintProp][132]?, maxFullscreenSize:
[ViewportSizeConstraintProp][132]?, children: React.Node?}

##### Properties

-   `minSize` **[ViewportSizeConstraintProp][132]?**
-   `maxFullscreenSize` **[ViewportSizeConstraintProp][132]?**
-   `children` **React.Node?**

#### ViewportSizeConstraintProp

Type: {width: ([number][67] | null)?, height: ([number][67] | null)?}

##### Properties

-   `width` **([number][67] | null)?**
-   `height` **([number][67] | null)?**

[1]: #airtableblocks
[2]: #blocksdk
[3]: #globalconfig
[4]: #globalconfigvalue
[5]: #viewport
[6]: #airtableblocksmodels
[7]: #fieldtypes
[8]: #generateguid
[9]: #viewtypes
[10]: #recordcoloring
[11]: #settingsbutton
[12]: #abstractmodel
[13]: #abstractmodelwithasyncdata
[14]: #aggregator
[15]: #base
[16]: #cursor
[17]: #field
[18]: #queryresult
[19]: #record
[20]: #table
[21]: #view
[22]: #watchable
[23]: #airtableblocksui
[24]: #css--external-scripts
[25]: #color-utilities
[26]: #react-hooks-for-working-with-airtable-data
[27]: #expandrecord
[28]: #expandrecordlist
[29]: #expandrecordpickerasync
[30]: #globalalert
[31]: #autocompletepopover
[32]: #button
[33]: #cellrenderer
[34]: #choicetoken
[35]: #collaboratortoken
[36]: #colorpalette
[37]: #confirmationdialog
[38]: #dialog
[39]: #fieldicon
[40]: #fieldpicker
[41]: #icon
[42]: #input
[43]: #link
[44]: #loader
[45]: #popover
[46]: #progressbar
[47]: #radiosynced
[48]: #recordcard
[49]: #recordcardlist
[50]: #select
[51]: #selectbuttons
[52]: #tablepicker
[53]: #toggle
[54]: #tooltip
[55]: #viewpicker
[56]: #viewportconstraint
[57]: #base
[58]: #cursor
[59]: #globalconfig
[60]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/String
[61]: https://developer.mozilla.org/en-US/docs/Web/API/Window/localStorage
[62]: https://developer.mozilla.org/en-US/docs/Web/API/Window/sessionStorage
[63]: #viewport
[64]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Array
[65]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Boolean
[66]: #globalconfigvalue
[67]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Number
[68]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Object
[69]: #viewportaddmaxfullscreensize
[70]: #viewportminsize
[71]: #field
[72]: #view
[73]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Statements/function
[74]: #record
[75]: #table
[76]: #aggregator
[77]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Date
[78]: #color
[79]: #linkedrecordsqueryresult
[80]: #tableorviewqueryresult
[81]: https://developer.mozilla.org/docs/Web/API/HTMLStyleElement
[82]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Promise
[83]: https://developer.mozilla.org/docs/Web/API/HTMLLinkElement
[84]: https://developer.mozilla.org/docs/Web/API/HTMLScriptElement
[85]: #colors
[86]: #color
[87]: #rgb
[88]: #rgb
[89]: https://reactjs.org/docs/hooks-intro.html
[90]: #withhooks
[91]: #usewatchable
[92]: #userecordids
[93]: #userecordbyid
[94]: #queryresult
[95]: #usebase
[96]: #userecords
[97]: #useviewport
[98]: #watchable
[99]: https://reactjs.org/docs/react-api.html#reactsuspense
[100]: https://reactjs.org/docs/forwarding-refs.html
[101]: #autocompletepopoverprops
[102]: #autocompleteitem
[103]: #buttonprops
[104]: #cellrendererprops
[105]: #choicetokenprops
[106]: #collaboratortokenprops
[107]: #dialogprops
[108]: #fieldiconprops
[109]: #fieldpickerprops
[110]: #fieldpickersyncedprops
[111]: #iconprops
[112]: #inputprops
[113]: #inputsyncedprops
[114]: https://www.owasp.org/index.php/Reverse_Tabnabbing
[115]: #loaderprops
[116]: #popoverprops
[117]: #progressbarprops
[118]: #radiosyncedprops
[119]: #recordcardprops
[120]: #expandrecordopts
[121]: #recordcardlistprops
[122]: https://developer.mozilla.org/docs/Web/API/Event
[123]: #selectprops
[124]: #selectsyncedprops
[125]: #tablepickerprops
[126]: #tablepickersyncedprops
[127]: #toggleprops
[128]: #togglesyncedprops
[129]: #tooltipprops
[130]: #viewpickerprops
[131]: #viewpickersyncedprops
[132]: #viewportsizeconstraintprop
