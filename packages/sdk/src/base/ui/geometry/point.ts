/** @internal */
export default class Point {
    /** @internal */
    x: number;
    /** @internal */
    y: number;

    /** @internal */
    constructor(x: number, y: number) {
        this.x = x;
        this.y = y;
    }
}
