import {css} from 'emotion';

const cssHelpers = {
    TRUNCATE: css({
        overflow: 'hidden',
        textOverflow: 'ellipsis',
        whiteSpace: 'nowrap',
    }),
};

export default cssHelpers;
