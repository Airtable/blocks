/** @internal */
export default class Size {
    /** @internal */
    width: number;
    /** @internal */
    height: number;

    /** @internal */
    constructor(width: number, height: number) {
        this.width = width;
        this.height = height;
    }
}
