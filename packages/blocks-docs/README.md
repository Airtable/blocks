# blocks-docs

This package implements a service to generate the data file which powers developer-facing API
documentation on airtable.com. It is used by the "sdk" package and the "blocks-testing" package.
