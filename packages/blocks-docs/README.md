# blocks-docs

This package generates the data file which powers developer-facing API documentation on
airtable.com. It uses the source code in the "sdk" package and the "blocks-testing" package as
input.
