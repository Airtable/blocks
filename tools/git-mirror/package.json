{
    "name": "@airtable-blocks-internal/git-mirror",
    "version": "1.0.2",
    "description": "Mirror the repo to an external clone",
    "main": "index.js",
    "private": true,
    "bin": {
        "git-mirror": "./bin/git-mirror"
    },
    "scripts": {
        "airbuild": "echo 'no airbuild configured'",
        "test": "echo 'no tests configured'"
    },
    "dependencies": {
        "minimatch": "^3.0.4",
        "@airtable-blocks-internal/strip-comments": "^2.0.1"
    }
}
